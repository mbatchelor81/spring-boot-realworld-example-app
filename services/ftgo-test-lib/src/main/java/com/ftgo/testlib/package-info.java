/**
 * FTGO shared test utilities library.
 *
 * <p>Provides test data builders, fixtures, custom assertions, mock configurations, and
 * Testcontainers helpers for consistent testing across all FTGO microservices.
 *
 * <p>Packages:
 *
 * <ul>
 *   <li>{@code builders} — Builder-pattern classes for domain objects
 *   <li>{@code fixtures} — Pre-built domain objects for common scenarios
 *   <li>{@code assertions} — AssertJ custom assertions for domain types
 *   <li>{@code mocks} — Spring test configurations (security, etc.)
 *   <li>{@code containers} — Testcontainers factory methods
 * </ul>
 */
package com.ftgo.testlib;
