/**
 * FTGO shared domain model. Contains all JPA entities, Spring Data repositories, and domain
 * configuration shared across FTGO microservices.
 *
 * <p>Entities: Order, Consumer, Restaurant, Courier, MenuItem, OrderLineItem, OrderLineItems,
 * RestaurantMenu, DeliveryInformation, PaymentInformation, Plan, Action, ActionType, OrderRevision,
 * LineItemQuantityChange, OrderState, OrderMinimumNotMetException.
 *
 * <p>Repositories: OrderRepository, ConsumerRepository, RestaurantRepository, CourierRepository.
 */
package com.ftgo.domain;
