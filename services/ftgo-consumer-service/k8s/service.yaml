apiVersion: v1
kind: Service
metadata:
  name: ftgo-consumer-service
  labels:
    app: ftgo-consumer-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: ftgo-consumer-service
