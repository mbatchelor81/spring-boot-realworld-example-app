<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
  <include resource="com/ftgo/logging/logback-shared.xml"/>

  <!-- ============================================================ -->
  <!-- Production: JSON structured logging via async appender        -->
  <!-- ============================================================ -->
  <springProfile name="prod">
    <logger name="com.ftgo" level="INFO"/>
    <logger name="org.springframework" level="ERROR"/>
    <logger name="org.hibernate" level="ERROR"/>
    <root level="WARN">
      <appender-ref ref="ASYNC_JSON"/>
    </root>
  </springProfile>

  <!-- ============================================================ -->
  <!-- Staging / Docker / K8s: JSON structured logging               -->
  <!-- ============================================================ -->
  <springProfile name="staging | docker | k8s">
    <logger name="com.ftgo" level="INFO"/>
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <root level="WARN">
      <appender-ref ref="ASYNC_JSON"/>
    </root>
  </springProfile>

  <!-- ============================================================ -->
  <!-- Local development: human-readable console + rolling file      -->
  <!-- ============================================================ -->
  <springProfile name="!docker, !k8s, !prod, !staging">
    <logger name="com.ftgo" level="DEBUG"/>
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.hibernate" level="INFO"/>
    <root level="INFO">
      <appender-ref ref="MASKED_CONSOLE"/>
      <appender-ref ref="FILE"/>
    </root>
  </springProfile>
</configuration>
