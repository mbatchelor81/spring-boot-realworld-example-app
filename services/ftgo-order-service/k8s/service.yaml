apiVersion: v1
kind: Service
metadata:
  name: ftgo-order-service
  labels:
    app: ftgo-order-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: ftgo-order-service
