apiVersion: v1
kind: Service
metadata:
  name: ftgo-order-service
  labels:
    app.kubernetes.io/name: ftgo-order-service
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: ftgo-order-service
