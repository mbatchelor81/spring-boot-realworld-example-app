apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# EFK stack deployed independently in 'logging' namespace.
# Apply separately: kubectl apply -k deployment/kubernetes/overlays/staging/efk

namespace: logging

resources:
  - ../../../base/efk

patches:
  - path: ../patches/elasticsearch-ilm.yaml
